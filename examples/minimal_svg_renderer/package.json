{
  "name": "minimal-svg-renderer",
  "version": "1.1.10",
  "private": true,
  "description": "Public demo for svg from msagl-js",
  "main": "main.js",
  "scripts": {
    "build": "rm -rf dist && esbuild ./src/main.ts --outfile=dist/main.js --bundle --minify && cp src/index.html dist"
  },
  "author": "levnach@microsoft.com",
  "license": "MIT",
  "dependencies": {
    "@msagl/parser": "^1.1.10",
    "@msagl/renderer-svg": "^1.1.12",
    "msagl-js": "^1.1.10"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.5.5"
  }
}
