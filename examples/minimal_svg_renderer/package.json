{
  "name": "minimal-svg-renderer",
  "version": "1.1.17",
  "private": true,
  "description": "Public demo for svg renderer",
  "main": "main.js",
  "scripts": {
    "build": "rm -rf dist && esbuild ./src/main.ts --outfile=dist/main.js --bundle --minify && cp src/index.html dist"
  },
  "author": "levnach@hotmail.com",
  "license": "MIT",
  "dependencies": {
    "@msagl/core": "^1.1.17",
    "@msagl/parser": "^1.1.17",
    "@msagl/renderer-svg": "^1.1.19"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.5.5"
  }
}
