{
  "name": "minimal-svg-renderer",
  "version": "1.1.11",
  "private": true,
  "description": "Public demo for svg from @msagl/core",
  "main": "main.js",
  "scripts": {
    "build": "rm -rf dist && esbuild ./src/main.ts --outfile=dist/main.js --bundle --minify && cp src/index.html dist"
  },
  "author": "levnach@microsoft.com",
  "license": "MIT",
  "dependencies": {
    "@msagl/parser": "^1.1.11",
    "@msagl/renderer-svg": "^1.1.13",
    "@msagl/core": "^1.1.10"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.5.5"
  }
}
