{
  "name": "minimal-renderer-webgl",
  "version": "1.1.16",
  "private": true,
  "description": "Public demo for webgl renderer for @msagl/",
  "main": "main.js",
  "scripts": {
    "build": "rm -rf dist && esbuild ./src/main.ts --outfile=dist/main.js --bundle --minify && cp src/index.html dist"
  },
  "author": "levnach@hotmail.com",
  "license": "MIT",
  "dependencies": {
    "@msagl/core": "^1.1.16",
    "@msagl/parser": "^1.1.16",
    "@msagl/renderer-svg": "^1.1.18"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.5.5"
  }
}
