{
  "name": "msagl-example",
  "version": "1.0.9",
  "private": true,
  "description": "Public demo for msagl-js",
  "main": "index.js",
  "scripts": {
    "start": "esbuild ./src/app.js --sourcemap --servedir=. --outfile=app.js --bundle",
    "build": "rm -rf dist && esbuild ./src/app.js --outfile=dist/app.js --bundle --minify && cp index.html dist"
  },
  "author": "xiac@microsoft.com",
  "license": "MIT",
  "dependencies": {
    "@msagl/parser": "^0.0.62",
    "@msagl/renderer": "^0.0.30",
    "msagl-js": "^0.0.62"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.5.5"
  }
}
